- name: Install and run the nginx
  hosts: frontend
  become: yes ##Providing sudo access
  tasks:
  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: Start nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
    
